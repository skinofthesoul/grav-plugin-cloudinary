{% extends 'partials/base.html.twig' %}

{% block content %}
  <h1>{{ page.title }}</h1>
  {#{ config.plugins.cloudinary.video }#}
  {{ cl_tag('video', page.header.public_id, page.header.options) }}
  {{ page.content }}
{% endblock %}
