enabled: true
cloud_name: your_cloud_name
key: your_key
secret: your_secret
unsigned_preset: unsigned_preset
defaults_video:
  video_options:
    width: 900
    video_codec: auto
    controls: true
  poster_options:
    fetch_format: auto
    start_offset: 2
    width: 900
defaults_image:
  options:
    width: 1280
    fetch_format: auto
